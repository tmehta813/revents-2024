export type Profile = {
    id: string,
    photoURL: string | null
    createdAt: string
    description: string
    displayName: string
}

export type Photo = {
    id: string
    name: string
    url: string 
}